{
  "name": "connect-four",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx wrangler pages dev --d1 DB=connectfour-ev -- vite",
    "build": "vite build",
    "preview": "vite preview --port 5173",
    "test": "vitest",
    "deploy": "npm run build && npx wrangler pages deploy dist"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
  "vite": "^8.0.0",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "vitest": "^1",
    "@vitejs/plugin-react": "^6.0.0",
    "concurrently": "^7.6.0"
  }
}
